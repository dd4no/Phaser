  // Instantiate Config Object
  var config = {
   type: Phaser.AUTO,
   width: 800,
   height: 600,
   scene: {
    preload: preload,
    create: create,
    update: update
   }
  };

  // Instantiate Game Object
  var game = new Phaser.Game(config);

  // Preload
  function preload() {

  }

  // Create
  function create() {

  }

  // Update
  function update() {

  }
